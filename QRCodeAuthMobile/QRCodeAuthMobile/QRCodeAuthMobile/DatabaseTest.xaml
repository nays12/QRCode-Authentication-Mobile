<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="QRCodeAuthMobile.DatabaseTest">
		<ContentPage.Content>

			<StackLayout BackgroundColor="Transparent" VerticalOptions="CenterAndExpand">

				<Label Text="Please select an option to view the content of the database in the debug window."
						   HorizontalOptions="CenterAndExpand"
						   Style="{StaticResource lblStyle}"/>

				<Button Text="Get Events"
						Clicked="GetEventsButtonClicked" 
						Style="{StaticResource buttonStyle}"/>

				<Button Text="Get Credentials"
						Clicked="GetCredentialsButtonClicked" 
						Style="{StaticResource buttonStyle}"/>

				<Button Text="Get Users"
						Clicked="GetUserssButtonClicked" 
						Style="{StaticResource buttonStyle}"/>
		</StackLayout>
			
		</ContentPage.Content>
	</ContentPage>